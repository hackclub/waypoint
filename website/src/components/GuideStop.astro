---
export interface Props {
  title?: string;
}

const { title = 'STOP' } = Astro.props;
---

<section class="guide-stop" role="alert" aria-label={title}>
  <div class="guide-stop__badge">{title}</div>
  <div class="guide-stop__content">
    <slot />
  </div>
</section>
